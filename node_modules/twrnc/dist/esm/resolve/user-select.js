import { complete } from '../helpers';
const ALLOWED_VALUES = [`auto`, `text`, `none`, `contain`, `all`];
export default function userSelect(value) {
    if (!ALLOWED_VALUES.includes(value))
        return null;
    return complete({
        userSelect: value,
    });
}
